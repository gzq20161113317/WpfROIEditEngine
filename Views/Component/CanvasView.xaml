<UserControl x:Class="RoiEditor.Views.Component.CanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RoiEditor.Views.Component" xmlns:controls="clr-namespace:RoiEditor.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <controls:RoiEditorCanvas Grid.Row="0"
                            ItemsSource="{Binding ROICollection}"
                            ROIOperationMode="{Binding CurrentOperationMode}"
                            SelectedRegions="{Binding MySelection}"
                            MapPath="{Binding MapPath}"
                            ActualMapBounds="{Binding ActualMapBounds,Mode=OneWayToSource}"
                            MaxLevel="{Binding MaxLevel, Mode=OneWayToSource}" 
                            CurrentLevel="{Binding CurrentLevel, Mode=OneWayToSource}"
                            EventAggregator="{Binding EventAggregator}"
                            ActiveROI="{Binding ActiveROI}"
                            SelectedROIRegion="{Binding SelectedROIRegion, Mode=TwoWay}"
                            Background="#1E1E1E"/>
        <StatusBar Grid.Row="1" Background="#333" Foreground="#CCC">
            <StatusBarItem>
                <TextBlock Text="{Binding LevelStatusString}" FontWeight="Bold" Foreground="White"/>
            </StatusBarItem>
            <Separator/>

            <StatusBarItem>
                <TextBlock Text="{Binding SelectedAreaInfo}" Foreground="#007ACC" FontWeight="Bold"/>
            </StatusBarItem>
            <Separator/>

            <StatusBarItem>
                <TextBlock Text="{Binding ActiveROIAreaInfo}"/>
            </StatusBarItem>
            <Separator/>

            <StatusBarItem>
                <TextBlock Text="{Binding TotalAreaInfo}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
