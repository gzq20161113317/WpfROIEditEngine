<Window x:Class="RoiEditor.Views.Component.AddROIView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:RoiEditor.Controls"
        Width="340" Height="300" 
        WindowStartupLocation="CenterOwner"
        Title="New ROI"
        Background="#252526" Foreground="White" ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ROI Name" Foreground="#999999" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Text="{Binding ROIName}" 
                 Height="28" VerticalContentAlignment="Center" Padding="5,0"
                 Background="#333337" Foreground="White" BorderBrush="#3F3F46"
                 Margin="0,0,0,15"/>

        <TextBlock Grid.Row="2" Text="Select Color" Foreground="#999999" Margin="0,0,0,5"/>

        <Border Grid.Row="3" Background="#333337" BorderBrush="#3F3F46" BorderThickness="1" CornerRadius="3" Padding="5" Margin="0,0,0,20">
            <controls:SimpleColorPicker SelectedColor="{Binding SelectedColor}" />
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="Confirm" Content="Create" Width="80" Height="28" Margin="0,0,10,0"
                    Background="#007ACC" Foreground="White" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#0099FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button x:Name="Cancel" Content="Cancel" Width="80" Height="28"
                    Background="#3E3E42" Foreground="White" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>